// Vite configuration intentionally disabled after migrating to Next.js.
// Keeping this file (without imports) so tools or scripts referencing it
// won't fail during the migration. If you no longer need it, delete it.

export default {};
